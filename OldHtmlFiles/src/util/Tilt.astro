---
import ImgTilt from '../../../src/app/gallery/imgTilt.js';

const options = {
  scale: 1.2,
  speed: 800,
  max: 30,
  glare: true
};
const { input, styling, bgImage } = Astro.props;
---

<html lang="en">
  <body>
    <ImgTilt
      className={styling}
      input={input}
      options={options}
      bgImage={bgImage}
      client:load
    />
  </body>
</html>
